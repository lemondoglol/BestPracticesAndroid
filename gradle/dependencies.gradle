def versions = [
        retrofit: "2.9.0",
        gson: "2.9.0"
]
ext {
    network = [
            retrofit : "com.squareup.retrofit2:retrofit:$versions.retrofit", // note double quarto here
            gson: "com.squareup.retrofit2:converter-gson:$versions.gson"
    ]
}